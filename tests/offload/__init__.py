"""Tests for the offload runner surface (``scripts/offload/*``).

The repo-root ``tests/conftest.py`` autouse fixture guards any test writing
under ``<repo>/data/`` — every offload test uses ``tmp_path`` instead.
"""
