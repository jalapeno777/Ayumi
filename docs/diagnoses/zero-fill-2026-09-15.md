# Zero-Fill Diagnosis — 2026-09-15

**Card:** 6e832de8-63f0-411c-8aac-c6132ffbc41d · **Author:** Ava · **Verdict: (a) CORRECT BEHAVIOR** — the load-bearing regime gate doing exactly its job. Not a bug. Not over-filtering.

## Evidence (all from live reads, 2026-09-15 ~16:05 UTC)

**All 12 signals today rejected by ONE gate — the regime gate:**

```
grep -c regime_trending_not_in_allowed logs/forward_test.log → 12
```

| Time (UTC) | Signal | Conf | Rejection |
|---|---|---|---|
| 08:15–10:30 (8×, every 15 min) | LONG XAUUSD SRMR+ (entry ~4262–4273, sl_dist 1.537) | 0.60–0.62 | regime_trending_not_in_allowed |
| 14:00, 14:15 (2×) | LONG XAUUSD SRMR+ | ~0.60–0.62 | regime_trending_not_in_allowed |
| +2 more (count total = 12) | same pattern | same | same |

- Signals were **healthy**: confidence 0.60-0.62 — well above the 0.40 floor; correctly adapted by the cTrader signal adapter (entry/SL present, sane sl_dist).
- The market regime classifier read **TRENDING** all session. SRMR+ is **QUIET-only allowed** — and that restriction is the Jul 22, 2026 finding that is LOAD-BEARING (adding CHOPPY destroyed PF 1.29→0.84). The gate held the line in a trending market. That is the designed behavior.
- Zero other rejections today: `grep "2026-09-15" forward_test.log | grep -icE "open risk|exceeds|timeout_await"` → **0**. The "$150 open-risk" rejection rows in `data/signal_stats.jsonl` are historical/harness artifacts, undated — not from today.

## Minor findings (carded separately)

1. **[FINDING] Regime-gate rejections are not logged to `signal_stats.jsonl`** — only to stdout log. Telemetry blind spot: the signal-stats spine (the live telemetry spine per the 2026-09-14 consolidation audit) misses an entire rejection class.
2. Same file contains rows with `timestamp/strategy/symbol = "unknown"` on the rejection path — a field-population bug on that logging branch (historical, undated rows).

## Gate-decision recommendation (due: 1 week under D5 elapsed since 2026-09-08)

**Recommendation: Option 1 — keep D5 and the regime gates exactly as they are.**

- The evidence says the stack is working as designed; today's zero fills are the QUIET-only gate correctly sitting out a trending market. SRMR+ historical character: 68% WR, PF 1.29, but ~71 trades/period — low volume is inherent, not a filter bug.
- Loosening the regime gate to chase fills would trade a validated edge for activity — precisely the Jul 22 failure mode.
- The volume problem (too few trades) is a **strategy-breadth problem**, and that's what the tournament matrix (card 05fa0065) exists to solve. Live XAUUSD SRMR+ continues as the execution proving ground.

**Options for Craig:**
1. **Keep as-is (recommended)** — live stays telemetry + execution proof; breadth comes from tournament.
2. **Add a second strategy to live** (e.g., donchian_atr_trend_v2 — TRENDING-affine, PF 1.03 baseline) so live has a leg that trades in trending regimes. Moderate risk: unvalidated pairing live; better after tournament verdict.
3. **Telemetry-only mode** — stop live entries entirely until tournament verdict. Zero risk, but loses execution-path validation we still need.
